<UserControl x:Class="Pastas.Presentation.Views.PreviewPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="420"
             d:DesignWidth="680">
    <Border Visibility="{Binding IsPreviewOpen, Converter={DynamicResource BoolToVisibility}}"
            Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource SubtleBorderBrush}"
            BorderThickness="1"
            CornerRadius="14"
            Padding="12">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
            <Grid>
                <TextBlock Text="Preview" Foreground="{DynamicResource CreamTextBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" />
            </Grid>
            <Border Grid.Row="1" Margin="0,10" Background="{DynamicResource SurfaceElevatedBrush}" CornerRadius="10" Padding="12">
                <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding SelectedItem.IsProtected}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style>
                            </StackPanel.Style>
                            <TextBlock Text="Protected item" Foreground="{DynamicResource CreamTextBrush}" FontSize="16" FontWeight="SemiBold" />
                            <TextBlock Text="Content is hidden" Foreground="{DynamicResource MutedTextBrush}" Margin="0,4,0,0" />
                        </StackPanel>

                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><MultiDataTrigger><MultiDataTrigger.Conditions><Condition Binding="{Binding SelectedItem.IsText}" Value="True" /><Condition Binding="{Binding SelectedItem.IsProtected}" Value="False" /></MultiDataTrigger.Conditions><Setter Property="Visibility" Value="Visible" /></MultiDataTrigger></Style.Triggers></Style>
                            </StackPanel.Style>
                            <TextBlock Text="{Binding SelectedItem.PreviewBodyText}" Foreground="{DynamicResource CreamTextBrush}" TextWrapping="Wrap" />
                        </StackPanel>

                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><MultiDataTrigger><MultiDataTrigger.Conditions><Condition Binding="{Binding SelectedItem.IsImage}" Value="True" /><Condition Binding="{Binding SelectedItem.IsProtected}" Value="False" /></MultiDataTrigger.Conditions><Setter Property="Visibility" Value="Visible" /></MultiDataTrigger></Style.Triggers></Style>
                            </StackPanel.Style>
                            <Image MaxHeight="340" Stretch="Uniform" Source="{Binding SelectedItem.ImagePath}">
                                <Image.Style>
                                    <Style TargetType="Image"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding SelectedItem.HasImagePath}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style>
                                </Image.Style>
                            </Image>
                            <StackPanel>
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding SelectedItem.HasImagePath}" Value="False"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style>
                                </StackPanel.Style>
                                <TextBlock Text="Image item" Foreground="{DynamicResource CreamTextBrush}" FontSize="16" />
                                <TextBlock Text="Image unavailable" Foreground="{DynamicResource MutedTextBrush}" Margin="0,4,0,0" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,2,0,0">
                    <Button Content="Copy" MinWidth="72" Style="{DynamicResource ChipButtonStyle}" Command="{Binding CopyItemCommand}" CommandParameter="{Binding SelectedItem}" />
                    <Button Content="Pin" MinWidth="60" Style="{DynamicResource ChipButtonStyle}" Command="{Binding TogglePinCommand}" CommandParameter="{Binding SelectedItem}" />
                    <Button Content="Del" MinWidth="60" Style="{DynamicResource ChipButtonStyle}" Command="{Binding DeleteItemCommand}" CommandParameter="{Binding SelectedItem}" Margin="0" />
                </StackPanel>
                <Button Grid.Column="1" Content="Back" MinWidth="72" Style="{DynamicResource ChipButtonStyle}" Command="{Binding ClosePreviewCommand}" Margin="0,2,0,0" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
