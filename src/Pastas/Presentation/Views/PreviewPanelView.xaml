<UserControl x:Class="Pastas.Presentation.Views.PreviewPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="420"
             d:DesignWidth="680">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>
    <Border Visibility="{Binding IsPreviewOpen, Converter={DynamicResource BoolToVisibility}}"
            Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource SubtleBorderBrush}"
            BorderThickness="1"
            CornerRadius="14"
            Padding="14">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
            <Grid Margin="0,0,0,4">
                <TextBlock Text="Preview" Foreground="{DynamicResource CreamTextBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" FontWeight="SemiBold" />
            </Grid>
            <Border Grid.Row="1" Margin="0,8,0,10" Background="{DynamicResource SurfaceElevatedBrush}" CornerRadius="12" Padding="16">
                <ScrollViewer Style="{DynamicResource ThemedScrollViewerStyle}">
                    <StackPanel>
                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding SelectedItem.IsText}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style>
                            </StackPanel.Style>
                            <TextBlock Text="{Binding SelectedItem.PreviewBodyText}" Foreground="{DynamicResource CreamTextBrush}" TextWrapping="Wrap" LineHeight="22" />
                        </StackPanel>

                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding SelectedItem.IsImage}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style>
                            </StackPanel.Style>
                            <Border Background="{DynamicResource InputBackgroundBrush}" BorderBrush="{DynamicResource SubtleBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="10">
                                <Image MaxHeight="340" Stretch="Uniform" HorizontalAlignment="Center" Source="{Binding SelectedItem.ImagePath}">
                                <Image.Style>
                                    <Style TargetType="Image"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding SelectedItem.HasImagePath}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style>
                                </Image.Style>
                                </Image>
                            </Border>
                            <StackPanel>
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding SelectedItem.HasImagePath}" Value="False"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style>
                                </StackPanel.Style>
                                <Border Background="{DynamicResource InputBackgroundBrush}" BorderBrush="{DynamicResource SubtleBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="14" Margin="0,6,0,0">
                                    <StackPanel>
                                        <TextBlock Text="Image item" Foreground="{DynamicResource CreamTextBrush}" FontSize="15" FontWeight="SemiBold" />
                                        <TextBlock Text="Image unavailable" Foreground="{DynamicResource MutedTextBrush}" Margin="0,4,0,0" />
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,2,0,0">
                    <Button x:Name="CopyButton" Content="Copy" MinWidth="70" Style="{DynamicResource ChipButtonStyle}" Click="CopyButton_OnClick" Margin="0,0,8,0" />
                    <Button MinWidth="66"
        Style="{DynamicResource ChipButtonStyle}"
        Command="{Binding TogglePinCommand}"
        CommandParameter="{Binding SelectedItem}"
        Margin="0,0,8,0">
    <Button.Content>
        <TextBlock Text="Pin">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedItem.IsPinned}" Value="True">
                            <Setter Property="Text" Value="Unpin" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Button.Content>
</Button>
                    <Button Content="Del" MinWidth="58" Style="{DynamicResource ChipButtonStyle}" Command="{Binding DeleteItemCommand}" CommandParameter="{Binding SelectedItem}" />
                </StackPanel>
                <Button Grid.Column="1" Content="Back" MinWidth="70" Style="{DynamicResource ChipButtonStyle}" Command="{Binding ClosePreviewCommand}" Margin="0,2,0,0" />
            </Grid>
        </Grid>
    </Border>
</UserControl>

